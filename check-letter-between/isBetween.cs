public static bool isbetween(char targetValue,char fisrtValue, char lastValue)
        {

            if (targetValue>=fisrtValue&&targetValue<=lastValue)
            {
                return true;
            }
            return false;
        }